services:
  - type: web
    name: comfort-routing
    env: python
    pythonVersion: "3.12"
    buildCommand: python -m pip install --upgrade pip setuptools wheel && python -m pip install -r requirements_minimal.txt
    startCommand: cd api && python -m uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: RENDER
        value: "true"
      - key: GRAPH_PATH
        value: data/雙北基隆路網_濃度與暴露_最大連通版.pkl
      - key: GOOGLE_API_KEY
        value: AIzaSyDnbTu8PgUkue5A9uO5aJa3lHZuNUwj6z0
    healthCheckPath: /health